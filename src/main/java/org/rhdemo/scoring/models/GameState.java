package org.rhdemo.scoring.models;

public enum GameState {
  // GAME_STATES = {
  // LOBBY: "lobby",
  // STOPPED: "stopped",
  // PAUSED: "paused",
  // LOADING: "loading",
  // ACTIVE: "active",
  // BONUS: "bonus"
  // };
  active, lobby, bonus, paused, stopped, loading
}
